import { Crumb } from './Crumb.ts'

/* class BackCrumb extends Crumb {
    static async fromFile (file: string) {
        const crumb = new BackCrumb()
        crumb._file = file
        crumb._code = await Deno.readTextFile(file)
        return crumb
    }
}
 */
